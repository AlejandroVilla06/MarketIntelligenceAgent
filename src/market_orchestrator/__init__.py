"""Market Orchestrator — hierarchical multi-market system.

This package is deliberately separate from src.agents/ to avoid the
heavy imports (chromadb, sentence_transformers) in agents/__init__.py.
"""
